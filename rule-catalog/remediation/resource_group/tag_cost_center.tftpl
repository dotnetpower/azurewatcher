# remediate.tag-add - Terraform patch template.
#
# Referenced by rule `resource-group.cost-center-tag.required`.
# Merge the cost-center tag.


resource "azurerm_resource_group" "${resource_id}" {
  tags = merge(
    var.tags,
    {
      "${tag_name}" = "${tag_value}"
    }
  )
}
