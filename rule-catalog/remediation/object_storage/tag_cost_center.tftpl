# remediate.tag-add — Terraform patch template.
#
# Referenced by rule `object-storage.cost-center-tag.required`.
# Merge the cost-center tag.


resource "azurerm_storage_account" "${resource_id}" {
  tags = merge(
    var.tags,
    {
      "${tag_name}" = "${tag_value}"
    }
  )
}
