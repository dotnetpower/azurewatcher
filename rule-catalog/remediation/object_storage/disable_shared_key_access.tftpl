# remediate.enable-rbac — Terraform patch template.
#
# Referenced by rule `object-storage.shared-key-access.disabled`.
# Turn off shared-key auth so every caller uses AAD RBAC.


resource "azurerm_storage_account" "${resource_id}" {
  shared_access_key_enabled = false
}
