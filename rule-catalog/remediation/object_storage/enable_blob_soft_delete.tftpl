# remediate.enable-backup-protection — Terraform patch template.
#
# Referenced by rule `object-storage.soft-delete-blob`.
# Turn on blob soft-delete with the operator-configured retention.


resource "azurerm_storage_account" "${resource_id}" {
  blob_properties {
    delete_retention_policy {
      days = ${retention_days}
    }
  }
}
