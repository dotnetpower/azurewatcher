# remediate.enable-backup-protection — Terraform patch template.
#
# Referenced by rule `sql-database.geo-redundant-backup`.
# Switch backup storage redundancy to geo.


resource "azurerm_mssql_database" "${resource_id}" {
  storage_account_type = "Geo"
}
