"""Local development tooling - CLIs, scripts, and reference implementations.

Not part of the shipped runtime - `tools/` is dev/CI only. Nothing under
this directory is installed into the wheel; it is invoked with
``python -m tools.<module>`` or as a plain script from the repo root.
"""
