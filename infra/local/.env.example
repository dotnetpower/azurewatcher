# Local dev stack env — copy to `.env` and adjust if needed.
# `.env` is git-ignored; this file is committed so a fresh clone knows the shape.
#
# ONLY dev defaults live here. Anything sensitive belongs in a secret store,
# never in a committed file (see .github/instructions/generic-scope.instructions.md).

POSTGRES_PASSWORD=devonly
