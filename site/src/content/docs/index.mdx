---
title: Forward Deployed Agents
description: Forward Deployed Agents - deterministic-first, event-driven, risk-gated cloud operations.
template: splash
hero:
  title: An autonomous agent org that lives in your cloud
  tagline: An org of 15 specialized AI agents that runs your cloud operations. Deploy the agent pantheon into your own environment and each agent takes its role - sensing, judging, executing, approving, auditing - so one org covers Cloud Ops and SRE, deterministic-first and risk-gated, with minimal human intervention.
  actions:
    - text: Get started
      link: /get-started/
      icon: right-arrow
      variant: primary
    - text: Read the roadmap
      link: /reference/roadmap/
      icon: right-arrow
      variant: secondary
    - text: View on GitHub
      link: https://github.com/dotnetpower/fdai
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import TrustTierFunnel from "../../components/TrustTierFunnel.astro";
import ActionOntologyExplorer from "../../components/ActionOntologyExplorer.astro";
import ControlLoopSteps from "../../components/ControlLoopSteps.astro";

<ul class="trust-bar">
  <li>Azure-native</li>
  <li>Deterministic-first</li>
  <li>Shadow-first, then enforce</li>
  <li>Every action audited</li>
  <li>Human-in-the-loop on high risk</li>
</ul>

## How Forward Deployed Agents resolve events

<p class="hero-note">One control loop, from signal to audit. Every event is normalised, routed by confidence, and gated by risk - and language models are reserved for the ambiguous few that the deterministic tiers deliberately defer.</p>

<ControlLoopSteps />

<TrustTierFunnel />

## What autonomy earns you

<ul class="impact-list">
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <strong>~70-80% of events resolved deterministically.</strong> Rules,
    policies, and checklists decide the repeatable majority. No model call.
  </li>
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <strong>~15-20% resolved by lightweight pattern reuse (T1).</strong>
    Embedding similarity to past resolved incidents, small classifiers, no
    frontier model.
  </li>
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <strong>Only ~5-10% reach the reasoning tier (T2).</strong> Mixed-model
    cross-check plus deterministic verifier - the model proposes, the
    verifier disposes.
  </li>
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <strong>Every autonomous action carries four invariants.</strong>
    Stop-condition · rollback path · blast-radius limit · audit-log entry.
    Missing any of the four ⇒ the action is HIL, not AUTO.
  </li>
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <strong>New capabilities land in shadow, never as a surprise.</strong>
    Promotion to enforce is a separate, measurable gate against the Phase 0
    baseline.
  </li>
  <li>
    <span class="impact-tick" aria-hidden="true">↳</span>
    <em>All figures above are design targets, not measured results.</em>
    Forward Deployed Agents never claim a multiplier without a paired baseline
    measurement.
  </li>
</ul>

## One core, three domains

<p class="hero-note">Each domain shares the same event-driven, risk-gated core; they differ only in the rules and actions they load.</p>

<CardGrid stagger>
  <Card title="Change Safety · Phase 1" icon="pencil">
    Rule catalog, T0 policy gate, remediation PRs. Change gate lands in shadow first, then promotes to enforce.
  </Card>
  <Card title="Resilience · Phase 3" icon="rocket">
    Scheduled resilience testing, DB DR drills, blast-radius-bounded chaos experiments - always with a stop-condition and rollback path.
  </Card>
  <Card title="Cost Governance · Phase 3" icon="approve-check">
    Cost anomaly detection, right-sizing PRs, budget guardrails per resource group. Auto vs HIL by risk classification.
  </Card>
</CardGrid>

## Meet the agent organization

<p class="hero-note">Fifteen named agents run the control plane as a single organization - each owns a role and hands off through a schema-checked event bus. It is an org chart, not a metaphor: the judge never executes, and every action is initiated, judged, approved, executed, and audited by distinct agents.</p>

<CardGrid>
  <Card title="Odin - command" icon="star">
    The master planner. Cross-vertical arbitration and the final tie-break before a verdict is finalised.
  </Card>
  <Card title="Judgment and execution" icon="approve-check">
    **Forseti** judges (auto / HIL / deny), **Thor** dispatches and executes, **Var** carries the human approval, **Vidar** owns rollback and DR failover. No agent both judges and executes.
  </Card>
  <Card title="Sensing" icon="magnifier">
    **Huginn** collects and correlates events; **Heimdall** watches health and drift. Deterministic-first - never an LLM in the hot path.
  </Card>
  <Card title="Conversation and audit" icon="comment">
    **Bragi** translates operator questions into tool calls; **Saga** writes the append-only audit trail and escalates handoffs.
  </Card>
  <Card title="Governance" icon="setting">
    **Mimir** stewards the rule catalog, **Norns** learns from outcomes, **Muninn** holds long-term memory.
  </Card>
  <Card title="Domain specialists" icon="puzzle">
    **Njord** (cost), **Freyr** (capacity), and **Loki** (chaos) advise the judge with domain analysis - they never execute directly.
  </Card>
</CardGrid>

## The action ontology

<ActionOntologyExplorer />

## Delivery phases

<p class="hero-note">Sequential by design - each phase builds on the one before it. Read the reference docs first, then follow the phases in order.</p>

<ol class="phase-timeline">
  <li>
    <span class="phase-marker" aria-hidden="true" />
    <div class="phase-label">Phase 0</div>
    <div class="phase-title">Instrument &amp; unblock</div>
    <p>KPI dashboard, baseline report, identity and policy blockers resolved.</p>
    <p class="phase-exit"><strong>Exit:</strong> reproducible baseline exists.</p>
  </li>
  <li>
    <span class="phase-marker" aria-hidden="true" />
    <div class="phase-label">Phase 1</div>
    <div class="phase-title">Rule catalog &amp; T0</div>
    <p>Rule catalog normalised, T0 policy gate live, remediation PRs generated automatically.</p>
    <p class="phase-exit"><strong>Exit:</strong> change gate runs in shadow across the target scope.</p>
  </li>
  <li>
    <span class="phase-marker" aria-hidden="true" />
    <div class="phase-label">Phase 2</div>
    <div class="phase-title">Quality gate &amp; T1</div>
    <p>Continuous rule update, LLM quality gate with mixed-model cross-check, embedding-based pattern reuse (T1).</p>
    <p class="phase-exit"><strong>Exit:</strong> auto-resolution rate validated vs the Phase 0 baseline.</p>
  </li>
  <li>
    <span class="phase-marker" aria-hidden="true" />
    <div class="phase-label">Phase 3</div>
    <div class="phase-title">Integrated autonomy</div>
    <p>Unified control loop, DR/Chaos scheduler with DB DR drills, FinOps auto-actions with risk-gated approvals.</p>
    <p class="phase-exit"><strong>Exit:</strong> autonomous MVP across all three domains.</p>
  </li>
  <li class="future">
    <span class="phase-marker" aria-hidden="true" />
    <div class="phase-label">Phase 4 · TBD</div>
    <div class="phase-title">Scale</div>
    <p>Continuous measurement, pattern-library growth, model cost/quality tracking, scalability. Multi-cloud expansion is TBD.</p>
  </li>
</ol>

## Ready to put the org to work?

<p class="hero-note">Deploy the agent organization into your own Azure environment, start every action in shadow, and promote to enforce once the numbers hold.</p>

<div class="cta-row">
  <a class="cta-btn cta-btn--primary" href={`${import.meta.env.BASE_URL}get-started/`}>Get started</a>
  <a class="cta-btn" href={`${import.meta.env.BASE_URL}reference/roadmap/`}>Read the roadmap</a>
  <a class="cta-btn" href="https://github.com/dotnetpower/fdai">View on GitHub</a>
</div>

:::note[Scope reminder]
This repo is **generic and customer-agnostic**. Everything is parameterised;
per-customer values live in a fork. Azure is the only implemented target - non-Azure
providers and Phase 4 multi-cloud expansion are TBD.
:::
